<h1>I am an attraction page</h1>

  <% if @user.admin %>
    <%= link_to "New Attraction", new_attraction_path %>
  <% end %>

<% @attractions.each do |a| %>
  <% if @user.admin %>
    <%= link_to "Show #{a.name}", attraction_path(a) %> <br>
  <% else %>
    <%= link_to "Go on #{a.name}", attraction_path(a) %> <br>
  <% end %>
<% end %>